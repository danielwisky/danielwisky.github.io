{
  "name": "danielwisky.github.io",
  "private": true,
  "scripts": {
    "build": "npm run build:js && npm run build:covers && npm run build:videos && npm run build:photos",
    "build:js": "node scripts/build-js.mjs",
    "build:covers": "node scripts/build-covers.mjs",
    "build:videos": "node scripts/build-video-thumbs.mjs",
    "build:photos": "node scripts/build-photos.mjs"
  },
  "devDependencies": {
    "terser": "^5.36.0",
    "sharp": "^0.34.0"
  }
}
